from Client import Client


class Collaborateur(Client):
    """classe qui modélise un collaborateur (rôle qui peut créer de
    nouveaux trajets ou lignes sur l'application)
    hérite de la classe Utilisateur
    """
